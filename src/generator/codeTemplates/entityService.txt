const { Service } = require('@core');
const path = require("path");

module.exports = class <%= className %>Service extends Service {

    constructor(app) {
        const domainPath = path.join(__dirname, './domain');
        super(app, domainPath);
    }

    get app() {
        return this.getApp();
    }
}
